// API service stub
const API_BASE = 'https://api.pawconnect.in';

export const apiClient = {
  get: async (endpoint) => {
    // stub
    return {};
  },
  post: async (endpoint, data) => {
    // stub
    return {};
  }
};

export default apiClient;

